fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12      # file uploads
pdfplumber==0.11.4             # PDF text + table extraction
PyMuPDF==1.24.11               # fallback for complex PDFs
pydantic[email]==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
sqlalchemy==2.0.35
alembic==1.13.3
aiosqlite==0.20.0              # async SQLite
asyncpg==0.29.0                # async PostgreSQL (used on Railway/prod)
greenlet==3.1.1                # required by SQLAlchemy async
httpx==0.27.2                  # download PDFs from URL
python-dotenv==1.0.1
pytest==8.3.3
pytest-asyncio==0.24.0

openai==1.51.0                 # SDK used to call GitHub Models API (NOT the OpenAI service)

# ── Billing ──────────────────────────────────────────────────
stripe==10.12.0               # billing (set STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET in .env)
